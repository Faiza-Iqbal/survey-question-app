import { QuestionStatement } from "./Title.style";

const QuestionTitle = () => {
  console.log("QuestionStatement");
  return <QuestionStatement> Select maximum 2 options </QuestionStatement>;
};

export default QuestionTitle;
